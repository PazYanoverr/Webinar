/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Stock } from "./Stock";
import { StockCountArgs } from "./StockCountArgs";
import { StockFindManyArgs } from "./StockFindManyArgs";
import { StockFindUniqueArgs } from "./StockFindUniqueArgs";
import { CreateStockArgs } from "./CreateStockArgs";
import { UpdateStockArgs } from "./UpdateStockArgs";
import { DeleteStockArgs } from "./DeleteStockArgs";
import { Company } from "../../company/base/Company";
import { Exchange } from "../../exchange/base/Exchange";
import { StockService } from "../stock.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Stock)
export class StockResolverBase {
  constructor(
    protected readonly service: StockService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Stock",
    action: "read",
    possession: "any",
  })
  async _stocksMeta(
    @graphql.Args() args: StockCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Stock])
  @nestAccessControl.UseRoles({
    resource: "Stock",
    action: "read",
    possession: "any",
  })
  async stocks(@graphql.Args() args: StockFindManyArgs): Promise<Stock[]> {
    return this.service.stocks(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Stock, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Stock",
    action: "read",
    possession: "own",
  })
  async stock(
    @graphql.Args() args: StockFindUniqueArgs
  ): Promise<Stock | null> {
    const result = await this.service.stock(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Stock)
  @nestAccessControl.UseRoles({
    resource: "Stock",
    action: "create",
    possession: "any",
  })
  async createStock(@graphql.Args() args: CreateStockArgs): Promise<Stock> {
    return await this.service.createStock({
      ...args,
      data: {
        ...args.data,

        company: args.data.company
          ? {
              connect: args.data.company,
            }
          : undefined,

        exchange: args.data.exchange
          ? {
              connect: args.data.exchange,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Stock)
  @nestAccessControl.UseRoles({
    resource: "Stock",
    action: "update",
    possession: "any",
  })
  async updateStock(
    @graphql.Args() args: UpdateStockArgs
  ): Promise<Stock | null> {
    try {
      return await this.service.updateStock({
        ...args,
        data: {
          ...args.data,

          company: args.data.company
            ? {
                connect: args.data.company,
              }
            : undefined,

          exchange: args.data.exchange
            ? {
                connect: args.data.exchange,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Stock)
  @nestAccessControl.UseRoles({
    resource: "Stock",
    action: "delete",
    possession: "any",
  })
  async deleteStock(
    @graphql.Args() args: DeleteStockArgs
  ): Promise<Stock | null> {
    try {
      return await this.service.deleteStock(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Company, {
    nullable: true,
    name: "company",
  })
  @nestAccessControl.UseRoles({
    resource: "Company",
    action: "read",
    possession: "any",
  })
  async getCompany(@graphql.Parent() parent: Stock): Promise<Company | null> {
    const result = await this.service.getCompany(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Exchange, {
    nullable: true,
    name: "exchange",
  })
  @nestAccessControl.UseRoles({
    resource: "Exchange",
    action: "read",
    possession: "any",
  })
  async getExchange(@graphql.Parent() parent: Stock): Promise<Exchange | null> {
    const result = await this.service.getExchange(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
